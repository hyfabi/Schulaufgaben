package at.maurutschek.test;

import java.io.File;

import at.maurutschek.myutil.Rekursionen;

public class Test {
	public static void main(String[] args) {
		Rekursionen r = new Rekursionen();
		File f = new File(System.getProperty("user.dir"));
		r.listFiles(new File(System.getProperty("user.dir")), "");
		r.getGroesse(System.getProperty("user.dir"));
	}
}
